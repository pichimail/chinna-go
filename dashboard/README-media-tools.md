# Chinna media tools

The media preview/editor tools are implemented inline in `dashboard/index.html` so the existing installer continues to force-refresh one dashboard file. This marker documents the dashboard feature area.
